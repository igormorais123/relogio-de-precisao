// Surface finish pass for the lesson car (paint depth, rims, brakes, tyre sidewalls).
// Contract: enhanceCar({model, mechanics, mobile}) -> {update(dt, time, pose), dispose()}.
export function enhanceCar() {
  return {update() {}, dispose() {}};
}
