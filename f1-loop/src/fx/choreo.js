// Motion choreography layered on the mechanics: part spin during explode, car "in action" cues.
// Contract: createChoreo({model, mechanics, camera, mobile}) -> {update(dt, time, pose), dispose()}.
export function createChoreo() {
  return {update() {}, dispose() {}};
}
